services:
  - type: web
    name: educalendar-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    rootDirectory: .  # Add this line - tells Render to use root directory
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000